# v0.2.0 - Graphical improvements

- **Ajustes gráficos `[video]`** en `config.ini`: ventana (borderless/windowed), resolución
  (auto/nativa/`<n>`/4K/8K), aspecto y MSAA.
- **Widescreen**: *snap* del *scissor* de overscan a pantalla completa; con `aspect=auto`/`expand` el
  3D llena la ventana (`HH_FULL_FRAME=0` lo desactiva).
- Builds Windows/Linux y CI/releases reproducibles.

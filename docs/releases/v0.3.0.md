# v0.3.0 — Widescreen HUD anchoring & minimap fix

Primera release con el HUD/minimapa **anclados** en widescreen.

- **Widescreen (fase 07b)**: radar y HUD de combate (POWER/STAMINA) anclados a la izquierda;
  **minimapa** anclado a la derecha, con el **fondo negro** del panel ajustado y recortado
  (el contenido ya no se sale del borde).
- **Cursor** oculto sobre la ventana del juego (se vuelve a mostrar al perder el foco).
- **Carpeta `rom/`** en los artefactos, con `PON_AQUI_LA_ROM.txt`, para colocar la ROM.
- **README** renovado: características y punto de control de estado.
- Builds **Windows (`.zip`)**, **Linux (`.tar.gz`)** e imagen Docker.
